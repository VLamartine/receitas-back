import { ShoppingListItem } from './shopping-list.item.entity';

describe('ShoppingListItemEntity', () => {
  it('should be defined', () => {
    expect(new ShoppingListItem()).toBeDefined();
  });
});
